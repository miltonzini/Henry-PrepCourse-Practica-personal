// Ejemplo 6
// imprimir números que cumplan la condición (ser 3);
var numeros = [1, 2, 3, 4];
numeros.forEach((num) => {
    if(num === 3){
        console.log(num);
    }
});
// probar luego hacer una variación en la que, dado un arreglo con diferentes valores, me devuelva solo los números 
// (en caso de que los hubiera)
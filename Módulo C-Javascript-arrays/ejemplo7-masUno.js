// Ejemplo 7
// dado un array de números, devolver otro en el que cada valor sea incrementado en uno.
var numeros = [1, 3, 9, 12];
var masUno = numeros.map((num) => {
   return num + 1;
});
console.log(masUno);
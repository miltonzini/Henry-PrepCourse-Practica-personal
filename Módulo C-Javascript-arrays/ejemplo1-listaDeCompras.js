// Ejemplo 1
// agregar elementos a un array en diferentes índices, retornarlo y retornar la cantidad de elemntos (incluídos los vacíos)
var listaDeCompras = [];
listaDeCompras[1] = 'lechuga'; 
listaDeCompras[3] = 'tomates';

console.log(listaDeCompras);

var elementoDelArray = listaDeCompras[1];
console.log(elementoDelArray);

console.log("'listaDeCompras' tiene " + (listaDeCompras.length) +  " elementos.");